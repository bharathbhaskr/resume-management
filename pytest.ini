# pytest.ini
[pytest]
# disable pytest-asyncio so it won’t try to collect packages as async tests
addopts = -p no:pytest_asyncio
# tell pytest where your tests live (optional, but can help)
testpaths = tests
python_files = test_*.py
